{
  "environment": "test",
  "workspaceId": "${PBI_WORKSPACE_ID}",
  "workspaceName": "${PBI_WORKSPACE_NAME}",
  "datasetName": "Vendas_Test",
  "servicePrincipalId": "${PBI_SP_TEST_CLIENT_ID}",
  "isolation": {
    "mode": "rls+build",
    "scope": "dataset-suffix",
    "note": "Workspace unico dinamico por projeto conectado; desvio por branch/sufixo no commit. Sem workspace por ambiente (derroga ADR-002)."
  },
  "refreshPolicy": {
    "type": "scheduled",
    "schedule": "0 */4 * * *",
    "maxRetries": 3
  },
  "approvers": [
    "qa-lead@empresa.com",
    "lider-tecnico@empresa.com"
  ],
  "alerts": {
    "teams": "https://outlook.office.com/webhook/PLACEHOLDER",
    "emailOnFailure": ["oncall-bi@empresa.com", "qa-lead@empresa.com"]
  }
}
